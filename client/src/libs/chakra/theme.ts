import { extendTheme } from "@chakra-ui/react";

const chakraTheme = extendTheme({
  colors: {
    // theme color
    activeColor: "#6ca4db",
    // text color
    subInfoText: "gray",
  },
});

export default chakraTheme;
